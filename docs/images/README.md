# docs/images

Store reusable question and teaching images here.

In **Edit Images**, type a filename and click **Load from Folder** to use files from
`docs/images/` (example: `docs/images/smear-01.jpg`).

Uploaded files (drag/drop or file picker) are now saved in your browser local storage,
so they remain after refresh on the same browser/device.
